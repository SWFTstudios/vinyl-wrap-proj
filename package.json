{
  "name": "vinyl-wrap-proj",
  "version": "1.0.0",
  "private": true,
  "description": "Apex Skin Studio — motorcycle vinyl wrap media brand site on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^22.15.32",
    "typescript": "^5.8.3",
    "wrangler": "^4.24.0"
  }
}
